# [PoLS Italy website](http://pols_italy.unimi.it/)

Requires **Ruby v.2.7**,
It is suggested to use rbenv (`$ sudo apt install rbenv`) and running:\  
`$ rbenv install 2.7.0`    
`$ rbenv local 2.7.0`

